---
name: paper-experimental-design
description: "Use when extracting experimental designs from papers."
---

# Paper experimental design (lab jail)

Citable design matrix. Experiment identity from shared design. Structured `n` lives only here.

Do not copy analogical `n`. Occupancy/atlas catalogs are not experimental designs.

Admit records into `logic.jsonl` via `lit-review`.
