# Include-what (lab jail)

Engine: `/opt/litreview`. HTTP: `POST /api/v1/tools/paper-decomposition/admit`.

This skill inventories sources. Panel semantics and Methods decomposition: `paper-logical-reconstruction`. Design matrix: `paper-experimental-design`.

Fill: source identity, accessible artifacts, figure/panel-candidate and Methods-span inventories, admit into `data/logic.jsonl`.

Each recipe slot: `stated | inferred | not_reported | source_unavailable | pending`.
